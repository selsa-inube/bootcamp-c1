//declaracion de variable.
let row = "";


//bucle en base a la cantidad de digitos.
for (let i = row.length; i < 7; i++) {

    //concatenacion de signos numeral.
    row = row + "#";

    //ejecucion de resultados en la consola.
    console.log(row);
}